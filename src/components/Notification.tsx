const Notification = () => {
    return (
        <div
            className="h-12 bg-blue-500 text-white px-4 flex items-center 
        justify-center text-center text-sm md:text-base font-semibold cursor-pointer"
        >
            Everything you need to enjoy the event is included.
        </div>
    );
};

export default Notification;
